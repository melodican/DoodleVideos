"""[3] SCRIPT — 18-24 min narration following the channel structure.

Sensational hook + escalating beats + speculative climax, but every factual
claim maps to a grounded source from research.gather().
"""
from __future__ import annotations


def write(title: str, sources: list[dict]) -> str:
    """Return narration script. TODO: LLM call with prompts/script.md."""
    raise NotImplementedError("Generate grounded script via LLM.")
