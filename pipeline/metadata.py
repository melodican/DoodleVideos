"""[7] METADATA — SEO title, description (with source links), tags, chapters."""
from __future__ import annotations


def build(title: str, script_text: str, sources: list[dict]) -> dict:
    """Return {description, tags, chapters}. TODO: LLM + source link block."""
    raise NotImplementedError("Build description/tags/chapters.")
